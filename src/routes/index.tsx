import { BrowserRouter } from 'react-router-dom';

import Routes from './routes';

const AppRoutes = () => {
  return (
    <BrowserRouter>
      <Routes />
    </BrowserRouter>
  );
};

export default AppRoutes;
